import { MILLI_SECS_METRIC_UNIT, COUNT_METRIC_UNIT } from '../common/Constants';

/**
 * Enum to map the field type to the field type prefix.
 */
export enum MetricUnits {
    MILLI_SECS = MILLI_SECS_METRIC_UNIT,
    COUNT = COUNT_METRIC_UNIT
}
